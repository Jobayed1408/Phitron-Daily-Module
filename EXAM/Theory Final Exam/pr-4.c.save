#include<stdio.h>

void freq(char str[100], int n)
{
    int frequency_array[26];
    for(int i=1; i<=26; i++)
    {
        frequency_array[i]=0;
    }
    for(int i=0; i<len; i++)
    {
        frequency_array[(str[i]-'a')+1]++;
    }
    for(int i=1; i<=26; i++)
    {
        int max=0;
        if(frequency_array[i]>max)
        {
            max = frequency_array[i];
            while(max!=0)
            {
                char ch;
                ch=i+96;
                printf("%c",ch);
                max--;
            }
        }
    }
}

void solve()
{
    char str[100];
    int a;
    scanf("%s",str);
    int len = strlen(str);
    
    printf("\n");
}
int main()
{
    solve();
}
