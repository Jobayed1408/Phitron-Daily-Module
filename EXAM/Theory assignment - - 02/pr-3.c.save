#include<stdio.h>
int is_one(char s[],char n){
    int flag = 0,i=0;
    while(s[i]!='\0'){
        if(s[i]=='n') flag=1;
        printf("%c ",s[i]);
        i++;
    }
    printf("%d\n",flag);
    return flag;
}


int main()
{
    char s[100];
    scanf("%s",s);
    if(is_one(s,'1') ){
        printf("Yes\n");
    }
    else
        printf("No\n");
}
