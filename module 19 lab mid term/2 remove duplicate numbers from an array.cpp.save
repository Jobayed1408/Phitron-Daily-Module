#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int v[n];
    for(int i=0; i<n; i++)
    {
        cin>>v[i];
        
    }
    vector<int > freq(n);
//    map<int , bool> m;
    for(int i=0; i<n ;i++)
    {
        
    }
    
    sort(freq.begin() , freq.end());
    
    
    for(int i=1;i<=n;i++)
    {
//        if(freq[i]>0)
        cout<< freq[i] << " ";
    }
}
