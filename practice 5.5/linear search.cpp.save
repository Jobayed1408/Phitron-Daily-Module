#include<bits./stdc++.h>
using namespace std;

void linear(int a[], int n, int k)
{
    int f=0;
    for(int i=0;i<n;i++)
    {
        if(a[i]==k) f=1; cout<<"Yes\n";
        else cout<<"No\n";
    }
}

int main()
{

    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    int k;
    cin>>k;
    for(int i=0;i<k;i++)
    {
        int s;
        cin>>s;
        linear(a,n,s);
    }
}
